node_modules/
dist/
.env
.env.local

# runtime state
data/*.json
!data/.gitkeep

# secrets
creds/
*.pem
*-sa.json

# user-specific config (may contain ${} that resolve to secrets, or inline keys)
config/pushers.config.yaml
config/*.local.yaml
launchd/*.local.plist

# editor
.vscode/
.idea/
.DS_Store

# logs
*.log
logs/
